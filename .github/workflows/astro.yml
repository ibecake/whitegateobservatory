name: WHITEGATE_OBS_WF
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours at minute 0
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build site (astro + weather)
        env:
          METEOSOURCE_API_KEY: ${{ secrets.METEOSOURCE_API_KEY }}
          WORLD_TIDES_KEY: ${{ secrets.WORLD_TIDES_KEY }}
        run: |
          rm -rf dist
          mkdir -p dist/astro dist/weather
          
          # Build cards (these create card.html files with actual API data)
          # astro_build.py generates all outputs in a single pass, reusing polled data:
          #   dist/astro/card.html, dist/astro/astro.json
          #   dist/weather/forecast.html
          #   dist/marine.html
          python astro_build.py --out dist/astro
          
          # Copy generated card.html to expected iframe locations
          # astro/card.html → astro/astro.html
          cp dist/astro/card.html dist/astro/astro.html
          # marine.html is generated by astro_build.py
          
          # Copy site shell pages (now only index.html - all forecasts consolidated)
          cp index.html dist/index.html
          cp radio.html dist/radio.html
          cp radio-astronomy.html dist/radio-astronomy.html
          cp astro-photography.html dist/astro-photography.html
          cp tinygs.html dist/tinygs.html
          cp media-manifest-builder.html dist/media-manifest-builder.html
          cp admin.html dist/admin.html
          # marine.html is generated by astro_build.py in dist/ already
          cp -R assets dist/assets
          cp -R astrophoto dist/astrophoto
          cp -R astroradio dist/astroradio
          cp -R radio dist/radio

          # Build stamp shown in the site footer (assets/js/build-stamp.js reads this)
          printf '{"built_at":"%s","commit":"%s","repo_url":"%s"}\n' \
            "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            "$(echo "${GITHUB_SHA}" | cut -c1-7)" \
            "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" > dist/build.json
          echo "=== build.json ==="
          cat dist/build.json

          # Debug: List what was created
          echo "=== dist/ structure ==="
          find dist -type f \( -name "*.html" -o -name "*.json" \) | sort
          echo "======================="
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist
          name: github-pages

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages
