# Radio gallery

Media for the Radio page (`radio.html`) is catalogued in `manifest.json`.
Binaries live in the public GCS bucket `whitegate-radio`.

Use [admin.html](../admin.html) to upload files, tag entries, and update this manifest.
