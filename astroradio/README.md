# Radio astronomy gallery

Media for the Radio Astronomy page (`radio-astronomy.html`) is catalogued in
`manifest.json`. Binaries live in the public GCS bucket `whitegate-astroradio`.

Use [admin.html](../admin.html) to upload files, tag entries, and update this manifest.
